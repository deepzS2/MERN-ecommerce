import type { NextPage } from 'next'
import Head from 'next/head'
import Link from 'next/link'
import { useRef } from 'react'
 import { MdClose } from 'react-icons/md' 
import { toast } from 'react-toastify'

import Button from '@components/Button'
import Modal, { ModalRef } from '@components/Modal'
import {
	Container,
	Description,
	Main,
	Title,
	ModalContent,
	ButtonsWrapper,
} from '@styles/pages/Home'

const Home: NextPage = () => {
	const modalRef = useRef<ModalRef>()

	return (
		<Container>
			<Head>
				<title>frontend/src/pages project</title>
				<meta name="description" content="Generated by create next app" />
				<link rel="icon" href="/favicon.ico" />
			</Head>

			<Main>
				<Modal ref={modalRef}>
					<ModalContent>
						<p>Hello World! I&apos;m a Modal Component!</p>

						<div className="buttons">
							<Button
								onClick={() => modalRef.current?.close()}
								padding=".75rem 2rem"
								pointer
								bgColor="red"
							>
								Close  <MdClose size={20} style={{ marginLeft: '.5rem' }} /> 
							</Button>
						</div>
					</ModalContent>
				</Modal>

				<Title>
					Bem vindo ao <a href="https://nextjs.org">Next.js!</a>
					<br />
					<span>com o template providenciado pelo deepz (csharpin)</span>
				</Title>

				<Description>
					Comece editando o arquivo <code>pages/index.tsx</code>
				</Description>

				<ButtonsWrapper>
					<Button onClick={() => modalRef.current?.open()} pointer>
						Clique para abrir o modal
					</Button>
					<Button
						onClick={() =>
							toast.info("I'm a toast :D", {
								position: 'top-right',
								autoClose: 5000,
								hideProgressBar: false,
								closeOnClick: true,
								pauseOnHover: true,
								draggable: true,
							})
						}
						pointer
						inverted
					>
						Clique para abrir o toast
					</Button>
				</ButtonsWrapper>
        
			</Main>
		</Container>
	)
}

export default Home
